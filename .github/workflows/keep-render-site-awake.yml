name: Keep Render Site Awake

on:
  schedule:
    - cron: "*/5 * * * *" # Har 5 minute
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Notify Bot Activation
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
            -d parse_mode="MarkdownV2" \
            -d text="✅ *KxShare Bot Activated!*\n\n🧠 _Monitoring system engaged_\n\n⚙️ *Keep-Alive Sequence Running...*\n\n🔄 Every 5 min check enabled\n\n👾 *Bot is now ALIVE & WATCHING* 👁️‍🗨️" || echo "Failed to send Telegram notification"

      - name: Curl Ping to KxShare
        run: |
          echo "Running 5-min check..."
          response=$(curl -s -o /dev/null -w "%{http_code}" --max-time 10 https://kxshare.onrender.com || echo "Curl failed")
          echo "HTTP Response: $response"

          if [ "$response" != "200" ]; then
            curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
              -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
              -d parse_mode="MarkdownV2" \
              -d text="🚨💥 *KxShare is DOWN!* 💥🚨\n\n*HTTP Response:* $response\n\n💀⚡ _System alert triggered!_ ⚡💀\n\n🔓💻 _Hacker alert!_ 💻🔓\n\n⚠️ _Fix it quickly before it's too late!_ ⚠️" || echo "Failed to send Telegram alert"
          fi
